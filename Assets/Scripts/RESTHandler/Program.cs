using System;
using Assets.Scripts.RESTHandler;

namespace WebApiHandler
{
    class Program
    {
        static void Main(string[] args)
        {
            //var proxiedObj = WebApiHandlerBuilder.ConstructWebApiHandler<SomeInterface>(null, null);
            //proxiedObj.TestMethod(fullUrl => Console.WriteLine(fullUrl), "users");
        }
    }
}
